# Builds the Flutter example app (gearup_scan/example) as a release APK.
# Run manually: Actions → "Build Gearup Scan APK" → Run workflow.
# Download the "gearup-scan-apk" artifact and install on your phone.
name: Build Gearup Scan APK

on:
  workflow_dispatch:

jobs:
  apk:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: gearup_scan/example
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Generate Android project
        run: flutter create . --project-name gearup_scan_example --org com.gearup

      - name: Dependencies
        run: flutter pub get

      - name: Allow HTTP to dev ERP (test installs only)
        run: |
          MAN=android/app/src/main/AndroidManifest.xml
          if grep -q 'usesCleartextTraffic' "$MAN"; then exit 0; fi
          sed -i 's/<application/<application android:usesCleartextTraffic="true" /' "$MAN"

      - name: Build APK
        run: flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: gearup-scan-apk
          path: gearup_scan/example/build/app/outputs/flutter-apk/app-release.apk
